MODEL:
  WEIGHTS: "detectron2://ImageNetPretrained/MSRA/R-50.pkl"
  MASK_ON: True
  RESNETS:
    DEPTH: 50
DATASETS:
  TRAIN: ("liposome_train",)
  TEST: ("liposome_val",)
SOLVER:
  IMS_PER_BATCH: 2
  BASE_LR: 0.00025
  MAX_ITER: 10000
  STEPS: [6000, 9000]
  CHECKPOINT_PERIOD: 1000
INPUT:
  MIN_SIZE_TRAIN: (800,)
  MAX_SIZE_TRAIN: 1333
  MIN_SIZE_TEST: 800
  MAX_SIZE_TEST: 1333
TEST:
  EVAL_PERIOD: 1000
OUTPUT_DIR: "./Phase_3/output"
